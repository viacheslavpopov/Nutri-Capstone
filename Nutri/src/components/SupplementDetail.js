import React from 'react';
import PropTypes from 'prop-types';

function SupplementDetail(props) {
    // deconstruct contants from props
    // return (
    //   return display structure  
    // );
}

SupplementDetail.propTypes = {
    // pass in supplement as a prop
    // no functions needed, read only (no delete or edit functionality)
}

export default SupplementDetail;